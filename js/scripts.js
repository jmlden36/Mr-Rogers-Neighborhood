function inputChangeToRangeArray(input) {
  let inputArray = [];
  for (let i = 0; i <= input; i++) {
    inputArray.push(i);
  }
  return(inputArray);
};

function changeRangeArray(inputArray) {
  if (inputArray === 1); {
    return true;
  }
}
